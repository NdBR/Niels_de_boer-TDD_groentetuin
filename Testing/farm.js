const getYieldForPlant = plant => plant.yield;

const getYieldForCrop = (input) => 0;

const getTotalYield = (crops) => 1;

module.exports = {
    getYieldForPlant,
    getYieldForCrop,
    getTotalYield
}